# Claude Code — MeWorld Game

**BEFORE ANY TASK: Read `C:\Users\steve\MeWorld\CURSOR_RULES.md` in full.**

That file is the single source of truth for:
- Which files are real vs reference-only
- Git / commit rules
- Data field rules (HPI spoiler prevention, exam orders, etc.)
- Icon system (Tabler only)
- Rollback / backup rules
- Folder structure

Do not skip this step. Do not assume you remember it from a prior session.
